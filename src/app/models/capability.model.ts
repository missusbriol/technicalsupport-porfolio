/** icon is a short text/emoji mark shown in a small badge - swap for
 *  a real icon component if you add an icon library later. */
export interface Capability { icon: string; title: string; body: string; }
